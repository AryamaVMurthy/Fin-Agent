# Fin-Agent local credentials template.
# Copy to .env.local and fill values locally.
# Never commit .env.local.

# Optional runtime path override.
FIN_AGENT_HOME=.finagent

# OpenCode server auth (if you run opencode serve with password protection).
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD=

# OpenAI auth for Opencode-backed agent runtime.
# Preferred: authenticate through OpenCode itself:
#   opencode auth login openai
# or in TUI:
#   /connect -> OpenAI -> ChatGPT Pro/Plus (OAuth)
# Optional: use API key if OAuth is not used.
OPENAI_API_KEY=

# Optional Fernet key for encrypted connector session storage.
FIN_AGENT_ENCRYPTION_KEY=

# Kite/Zerodha credentials (for integration phase).
FIN_AGENT_KITE_API_KEY=
FIN_AGENT_KITE_API_SECRET=
# Must match Zerodha app redirect exactly.
# Default local callback:
# FIN_AGENT_KITE_REDIRECT_URI=http://127.0.0.1:8080/v1/auth/kite/callback
FIN_AGENT_KITE_REDIRECT_URI=
FIN_AGENT_KITE_ACCESS_TOKEN=

# Provider rate-limits (fail-fast)
FIN_AGENT_RATE_LIMIT_KITE_MAX_REQUESTS=20
FIN_AGENT_RATE_LIMIT_KITE_WINDOW_SECONDS=1
FIN_AGENT_RATE_LIMIT_NSE_MAX_REQUESTS=10
FIN_AGENT_RATE_LIMIT_NSE_WINDOW_SECONDS=1
FIN_AGENT_RATE_LIMIT_TRADINGVIEW_MAX_REQUESTS=5
FIN_AGENT_RATE_LIMIT_TRADINGVIEW_WINDOW_SECONDS=1
